spring:
  datasource:
    url: jdbc:mysql://mysql:3306/booknest?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  server:
    port: 8585
